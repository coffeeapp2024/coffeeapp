export type Post = {
  imgUrl: string;
  liked: number;
};

export type BoostInfo = {
  imgUrl: string;
  balance: number;
  info: string;
  price: number;
};

export type GameInfo = {
  imgUrl: string;
  price: number;
};

export type VoucherInfo = {
  imageUrl: string;
  info: string;
  price: number;
};
